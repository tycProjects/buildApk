org.gradle.jvmargs=-Xmx1536m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

android.useAndroidX=true
android.nonTransitiveRClass=true

kotlin.code.style=official

# NOTE: android.aapt2FromMavenOverride is intentionally NOT set here.
# Valence's setup-termux-android.sh writes it into ~/.gradle/gradle.properties
# (Google's Maven aapt2 is x86-64-only — VALENCE-DOCS §15/§17).
# Do not duplicate it in-project; that would shadow the Termux override.
android.enableJetifier=true
